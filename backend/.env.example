# Backend Environment Variables
# Copy this file to .env and fill in your values

# Server
PORT=5000
NODE_ENV=development

# Database (Supabase PostgreSQL)
DATABASE_URL=postgresql://postgres:your_password@db.your-project.supabase.co:5432/postgres
postgresql://postgres:RW%kGY_cZP/K6t.@db.pydhxufnywggvcxkcoui.supabase.co:5432/postgres

# JWT
JWT_SECRET=your_jwt_secret_key_change_this_in_production
JWT_EXPIRES_IN=7d

# Stripe
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Firebase Admin (Service Account)
# For local dev, you can set this to a file path:
#   FIREBASE_SERVICE_ACCOUNT=./firebase-service-account.json
# For production hosts like Render, prefer base64-encoded JSON:
#   FIREBASE_SERVICE_ACCOUNT=eyJ0eXBlIjoi... (base64 of the service account JSON)
FIREBASE_SERVICE_ACCOUNT=

# CORS
# In production, set this to your deployed frontend origin (or Expo web/admin domain), e.g.
#   CORS_ORIGIN=https://your-admin.vercel.app
# Use '*' only if you do NOT need cookies/credentials from browsers.
CORS_ORIGIN=*
